name: CI
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install devbox
      uses: jetpack-io/devbox-install-action@v0.5.0
      with:
        refresh-cli: true
    # - run: devbox run npm install
    # - run: devbox run npm test
